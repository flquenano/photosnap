import React from "react";
import { GlobalStyles } from "../GlobalStyle.js";

import { Layout } from "./layout.css.js";

const LayoutComponent = ({ children }) => <Layout> {children}</Layout>;

export default LayoutComponent;
